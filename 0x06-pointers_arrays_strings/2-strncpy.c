#include "main.h"

/**
 * _strncpy - this functions copies the source string to the destination string
 *
 * @dest: the dest pointer pointing to where the copied string is stored
 * @src: the src pointer pointing to the source string to be copied
 * @n: n parameter represent the number of time the source will be appended
 *
 * Return: pointer dest
 */

char *_strncpy(char *dest, char *src, int n)
{

	strncpy(char *dest, char *src, n);

	return (dest;)
}
