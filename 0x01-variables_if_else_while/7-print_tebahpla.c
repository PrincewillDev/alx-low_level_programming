#include <stdio.h>

/**
 * main -entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int ascii_alphabets;

	for (ascii_alphabets = 122; ascii_alphabets >= 97; ascii_alphabets--)

		putchar(i);

	putchar('\n');

	return (0);

}
