#include <stdio.h>

/**
 * main -entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char alphabets;

	for (alphabets = "z"; alphabets >= "a"; alphabets--)

		putchar(i);

	putchar('\n');

	return (0);

}
