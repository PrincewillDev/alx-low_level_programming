#include <stdio.h>

/**
 * main -entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char alphabets;

	for (alphabets = "z"; i >= "a"; i--)

		putchar(i);

	putchar('\n');

	return (0);

}
