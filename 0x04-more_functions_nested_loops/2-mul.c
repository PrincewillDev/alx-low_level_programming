#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two integers
 * @a: integer variable
 * @b: integer variable
 * Return: 0 always (Success)
 */

int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
